import Foundation

/// Placeholder module — the preset format implementation lands in Phase 2
/// per `docs/specs/preset-format.md`.
public enum PresetsModule {
    public static let placeholder = "Presets module — Phase 2 stub"
}
