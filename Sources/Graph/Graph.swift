import Foundation

/// Placeholder module — the real graph implementation lands in Phase 2
/// per `docs/orchestration/phases/02-dsp-chain.md`.
public enum GraphModule {
    public static let placeholder = "Graph module — Phase 2 stub"
}
