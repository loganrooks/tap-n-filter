import Foundation

/// Placeholder module — the concrete effects land in Phase 2
/// per `docs/orchestration/phases/02-dsp-chain.md`.
public enum EffectsModule {
    public static let placeholder = "Effects module — Phase 2 stub"
}
